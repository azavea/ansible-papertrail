---
papertrail_version: "v0.13"
papertrail_check_interval_s: 10
papertrail_host: "logs2" # set to user's subdomain
papertrail_port: "" # set to user's port
papertrail_log_files:
  - /var/log/**/*.log
